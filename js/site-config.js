/* Host-managed settings. After creating the free Calendly event, paste its
   public URL here (for example: https://calendly.com/your-name/30min). */
window.QH_CONFIG = Object.freeze({
  calendarUrl: ""
});
